function ad()
{
    var a=parseInt(prompt('Enter a values :'));
    var b=parseInt(prompt('Enter b values :'));
    var res=a+b;
    alert('The additions of numbers is :'+res)
}

function dv()
{
    var a=parseInt(prompt('Enter a values :'));
    var b=parseInt(prompt('Enter b values :'));
    var res=a/b;
    alert('The Divisions of numbers is :'+res)
}